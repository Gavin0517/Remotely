@page "/"
@inject IApplicationConfig AppConfig
@inject IDataService DataService
 
<AuthorizeView>
    <NotAuthorized>
        <div class="jumbotron">
            <h3 class="display-4">E-Lab 远程</h3>
            <p class="lead">
                @*An open-source remote support solution.*@
            </p>
            <hr class="my-4">
            <p class="lead">
                <a class="btn btn-primary btn-lg mr-2" href="Identity/Account/Login" role="button">登录</a>

                @if (AppConfig.MaxOrganizationCount < 0 || DataService.GetOrganizationCount() < AppConfig.MaxOrganizationCount)
                {
                    <a class="btn btn-primary btn-lg" href="Identity/Account/Register" role="button">注册</a>
                }
            </p>
        </div>
    </NotAuthorized>
    <Authorized>
        <AuthorizedIndex />
    </Authorized>
</AuthorizeView>
